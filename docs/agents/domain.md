# Domain docs

This is a single-context project.

Before working on a feature, read root `CONTEXT.md` and any relevant records in `docs/adr/`.

Use the vocabulary defined in `CONTEXT.md` for issues, implementation, and tests. If no relevant ADR exists, proceed silently.
